language: python

python:
  - 3.6

build:
  pre_ci_boot:
    options: "-v /app:/app"
  ci:
    - cp -R . /app
    - pip install -U docker-compose
    - docker-compose up -d
    - sleep 10
    - ls -la
    - pwd
    - docker-compose down
