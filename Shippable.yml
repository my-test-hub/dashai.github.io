language: python

python:
  - 3.6

build:
  pre_ci_boot:
    options: "-v /app:/app"
  ci:
    - cp -R . /app
    - pip install -U docker-compose
    - docker-compose -f docker-compose-shippable.yml up -d
    - sleep 10
    - docker-compose -f docker-compose-shippable.yml logs
    - ls -la
    - pwd
    - docker-compose down
